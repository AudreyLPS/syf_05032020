
{% extends 'base.html.twig'  %}
{% block body %}
    <div class="row">
        <div class="col">      
            
  
<div id="carousel" class="carousel slide" data-ride="carousel">
  <ol class="carousel-indicators">
    <li data-target="#carousel" data-slide-to="0"></li>
    <li data-target="#carousel" data-slide-to="1"></li>
    <li data-target="#carousel" data-slide-to="2"></li>
    <li data-target="#carousel" data-slide-to="3"></li>
    <li data-target="#carousel" data-slide-to="4"></li>
    <li data-target="#carousel" data-slide-to="5"></li>
  </ol>
  <div class="carousel-inner">

    <div class="carousel-item active">
      <img  src="{{ asset('img/continent/asie.jpg') }}" class="d-block w-100" alt="...">
      <div class="carousel-caption d-none d-md-block">
        <h1>
            <a href="{{url('continent.index', { id:results.0.id })}}" class="font-weight-bold btn btn-secondary btn-lg btn-block">{{ results.0.name }}</a>
        </h1>
        </div>
    </div>
    <div class="carousel-item">
      <img src="{{ asset('img/continent/afrique.jpg') }}" class="d-block w-100" alt="...">
      <div class="carousel-caption d-none d-md-block">
        <h1>
            <a href="{{url('continent.index', { id:results.1.id })}}" class="font-weight-bold btn btn-secondary btn-lg btn-block">{{ results.1.name }}</a>
        </h1>
      </div>
    </div>
    <div class="carousel-item">
      <img src="{{ asset('img/continent/oceanie.jpg') }}" class="d-block w-100" alt="...">
      <div class="carousel-caption d-none d-md-block">
        <h1>
            <a href="{{url('continent.index', { id:results.2.id })}}" class="font-weight-bold btn btn-secondary btn-lg btn-block">{{ results.2.name }}</a>
        </h1>
    </div>
    </div>
    <div class="carousel-item">
      <img src="{{ asset('img/continent/amerique.jpg') }}" class="d-block w-100" alt="...">
      <div class="carousel-caption d-none d-md-block">
       <h1>
            <a href="{{url('continent.index', { id:results.3.id })}}" class="font-weight-bold btn btn-secondary btn-lg btn-block">{{ results.3.name }}</a>
        </h1>
      </div>
    </div>
    <div class="carousel-item">
      <img src="{{ asset('img/continent/antarctique.jpg') }}" class="d-block w-100" alt="...">
      <div class="carousel-caption d-none d-md-block">
       <h1>
            <a href="{{url('continent.index', { id:results.4.id })}}" class="font-weight-bold btn btn-secondary btn-lg btn-block">{{ results.4.name }}</a>
        </h1>
      </div>
    </div>
    <div class="carousel-item">
      <img src="{{ asset('img/continent/europe.jpg') }}" class="d-block w-100" alt="...">
      <div class="carousel-caption d-none d-md-block">
        <h1>
            <a href="{{url('continent.index', { id:results.5.id })}}" class="font-weight-bold btn btn-secondary btn-lg btn-block">{{ results.5.name }}</a>
        </h1>
      </div>
    </div>
  </div>
  <a class="carousel-control-prev" href="#carousel" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carousel" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
            </p>
        </div>
    </div>
{% endblock %}