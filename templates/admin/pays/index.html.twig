{% extends 'base.html.twig' %}

{% block body %}
	<div class="row">
		<div class="col">
			<h1>Gestion des etapes</h1>
		</div>
	</div>
	<div class="row">
	<div class="col-md-12">
		{% include "_inc/flash.message.html.twig" %}
	
    <p class="text-right">
        <a href="" class="btn btn-success"> Ajouter un pays</a>
    </p>
	</div>
		<table class="table table-striped">
			<tr>
				<th>Pays</th>
				<th>Nombre d'étape</th>
				<th>Actions</th>
			</tr>
			{% for pays in results %}
				<tr>
					<td>{{ pays.name }}</td>
					<td></td>
					<td>
						<a href="{{ url('admin.etape.form') }}" class="btn btn-info col-md-3">Ajouter une etape</a>
						<a href="" class="btn btn-warning col-md-3">Modifier</a>
						<a href="" class="btn btn-danger col-md-3">Supprimer</a>
					</td>
				</tr>
			{% endfor %}
		</table>
	</div>
{% endblock %}







